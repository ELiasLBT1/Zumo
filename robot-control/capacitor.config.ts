import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.robotcontrol.zumo',
  appName: 'ZUMO Robot Control',
  webDir: 'www'
};

export default config;
